% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_categorical_data.R
\name{generate_categorical_data}
\alias{generate_categorical_data}
\title{Generate Categorical Data}
\usage{
generate_categorical_data(n, levels, proportions)
}
\arguments{
\item{n}{The number of data points to generate.}

\item{levels}{A vector of category names (e.g., c("A", "B", "C")).}

\item{proportions}{A vector of proportions for each category. Must sum to 1 if using decimals, or 100 if using percentages.}
}
\value{
A factor vector with the generated categorical data.
}
\description{
This function creates random categorical data with specified levels and proportions.
Proportions can be specified as decimal fractions (e.g., 0.2, 0.5) or percentages (e.g., 20, 50).
}
\examples{
# Using decimal proportions
generate_categorical_data(100, c("A", "B", "C"), c(0.2, 0.3, 0.5))
# Using percentage proportions
generate_categorical_data(100, c("A", "B", "C"), c(20, 30, 50))
}
